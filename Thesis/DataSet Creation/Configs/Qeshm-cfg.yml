#jupyter nbconvert --execute --to html test-run-with-command-line.ipynb
#jupyter nbconvert --to ipynb --execute ./test-run-with-command-line.ipynb --output "1403-08-27.ipynb"
log:
    mode: both
    # Note: The name and path of the log file are set in the settings section for each code.
    filename: now # if set to "now", the current date set as the logfile name.
    filepath: None # Set in the subset config.
    level: INFO
    format: "%(asctime)s - %(levelname)s - %(message)s"

mk_dataset:
    cut_time:
        before: 5
        after: 25
    zero_pad:
        before: 0
        after: 0
    noisepad:
        before: 0
        after: 0
        std_start: 2
        std_end: 2
    filter:
        apply: False
        low: 1
        high: 10
    autolabeling: False
    save_streams: False
    path:
        inputs:
            catalog: X:\Catalogs\Qeshm.out
            stream_root: X:\Local_Network_Data-SeisComP\Qeshm
            network_details: X:\Network_Details\Qeshm.csv
            stream_pattern: '{time.year}/QM/*/HH*.D/*.*.*.*.D.{time.year}.{time.julday:03d}'
        outputs:
            dataset: 'Y:\DataSets\Qeshm-Dataset_{datetime_str}'
            log:
                filename: now

dataset_test:
    path:
        dataset: Y:\DataSets\Qeshm-Dataset_2025-04-12T14-50-37
